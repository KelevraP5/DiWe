// report_page.dart

import 'package:flutter/material.dart';

class ReportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page de bilan'),
      ),
      body: Center(
        child: Text('Contenu de la page de bilan'),
      ),
    );
  }
}
