// bolus_page.dart

import 'package:flutter/material.dart';

class BolusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page de bolus'),
      ),
      body: Center(
        child: Text('Contenu de la page de bolus'),
      ),
    );
  }
}
