import 'package:flutter/material.dart';

class MealPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page de repas'),
      ),
      body: Center(
        child: Text('Contenu de la page de repas'),
      ),
    );
  }
}
